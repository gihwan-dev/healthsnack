'use client';

import { useEffect } from 'react';

const AdminPage = () => {
  useEffect(() => {
    // 권환 학인 후 메인 페이지 또는 로그인 페이지 이동
  });
  return <section className="section section-padding">Loading...</section>;
};

export default AdminPage;
